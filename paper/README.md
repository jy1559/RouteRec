# Paper Assets

Use this directory for the final paper-facing artifacts only.

Recommended contents:

- final figure exports used in the paper
- table manifests or CSV exports that back the reported numbers
- checkpoint or artifact manifests, if checkpoints are released separately
- exact reproduction command summaries for headline results

Avoid copying draft history or LaTeX build byproducts here.
